declare class Voicea {
  constructor(token: string, meeting: any, sessionID: string)
  socket: any;
  token: string;
  sessionID: string;
  meeting: any;

  async generateSocketUrl(): Promise<string>;
  async connect(): Promise<void>;
  getCurrentSpeaker(csis): any;
  getTranscription(callback: (data: any)=>void): void;
  closeSocket(): void;
}

export default Voicea;
